What is virtual environment? 
Where did the `my_ai/.venv` go? Why did I delete it? 
Why is there suddenly a new `.venv` folder in `phase_00`? Why don't we just use a single virtual environment for the entire project?
What about the new `pyproject.toml` and `uv.lock` in `phase_00`?